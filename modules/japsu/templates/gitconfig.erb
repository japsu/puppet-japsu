# puppet

[user]
        name = Santtu Pajukanta
        email = <%= @email_address %>
[color]
        ui = auto
[alias]
        st = status
        prom = pull --rebase origin master
        rb = rebase
        co = checkout
        ci = commit
        ff = pull --ff-only
        aa = add -A
[push]
        default = simple
